src/
│
├── components/         
│   ├── ui/             # Small, reusable UI components (buttons, cards, etc.)
│   ├── layout/         # Layout components (Sidebar, Navbar, Footer)
│   └── common/         # Shared components used across pages
│
├── pages/              # Page-level components (Home, Projects, About, etc.)
│
├── hooks/              # Custom React hooks (useDarkMode, useFetch, etc.)
│
├── context/            # React Context Providers (AuthContext, ThemeContext)
│
├── services/           # API calls, Firebase, authentication logic, etc.
│
├── utils/              # Helper functions (formatDate, calculateAge, etc.)
│
├── assets/             # Images, icons, static files
│   ├── images/
│   └── icons/
│
├── styles/             # Global CSS, Tailwind config, theme files
│
├── constants/          # Constants, config objects, enums
│
├── App.jsx
└── main.jsx
